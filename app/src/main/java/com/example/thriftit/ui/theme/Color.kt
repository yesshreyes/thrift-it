package com.example.thriftit.ui.theme

import androidx.compose.ui.graphics.Color

// ---------- LIGHT THEME (soft & calm) ----------
val LightPrimary = Color(0xFF2F2F2F) // Dark grey (not pure black)
val LightOnPrimary = Color(0xFFFFFFFF)

val LightBackground = Color(0xFFF6F6F6) // Soft off-white
val LightSurface = Color(0xFFFFFFFF)

val LightOnBackground = Color(0xFF1E1E1E)
val LightOnSurface = Color(0xFF1E1E1E)

val LightSecondary = Color(0xFF6B6B6B) // Mid grey
val LightOnSecondary = Color(0xFFFFFFFF)

// ---------- DARK THEME (soft dark, no harsh black) ----------
val DarkPrimary = Color(0xFFE6E6E6) // Soft white
val DarkOnPrimary = Color(0xFF1A1A1A)

val DarkBackground = Color(0xFF121212) // Dark grey (not AMOLED black)
val DarkSurface = Color(0xFF1C1C1C)

val DarkOnBackground = Color(0xFFE0E0E0)
val DarkOnSurface = Color(0xFFE0E0E0)

val DarkSecondary = Color(0xFF9E9E9E)
val DarkOnSecondary = Color(0xFF1A1A1A)
